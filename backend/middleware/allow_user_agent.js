/**
 * @project Sistem Klinik
 * @file allow_user_agent.js
 * @description Middleware pengecekan User-Agent (dasar, siap dikembangkan)
 */

export const useragentMiddleware = (req, res, next) => {
  return next();
};
